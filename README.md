# Zenix LOL Vitrine

Static landing page that exposes the current active streaming URL.

## Update link
Edit `config.js`:
- `ZENIX_ACTIVE_URL`
- `ZENIX_LAST_UPDATED`

## Deploy (Render static site)
- Build command: none
- Publish directory: project root
